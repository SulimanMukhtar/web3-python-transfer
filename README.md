# web3-python-transfer


Installing pipenv using pip:

    pip install --user pipenv
activate environment:

    pipenv shell

install required dep:

    pipenv install

run after editing the main.py

    python3 main.py

